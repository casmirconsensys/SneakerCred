pragma solidity ^0.5.0;

Contract MyTeamUnlimited;

  Struct MyTeamCards {
    string Player1;
    string Player2;
    string Player3;
    string Player4;
    string Player5;
    uint Rating1;
    uint Rating2;
    uint Rating3;
    uint Rating4;
    uint Rating5;
  }
  mapping (address => player) members;
  mapping (address[] userIndex);
  mapping (address[i] userBalances);
  mapping (address Key = msg.sender);
  userIndex.push(anAddress);
  userBalances[key] = 100;
  uint count = userIndex.length;
  uint retrieveBalance = userBalances[address];

  Struct Attributes {
    uint Badge;
    uint PPG;
    uint RPG;
    uint SPG;
    uint APG;
    uint MPG;
  }
